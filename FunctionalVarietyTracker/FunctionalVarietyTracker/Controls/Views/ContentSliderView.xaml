<UserControl x:Class="FunctionalVarietyTracker.Controls.Views.ContentSliderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FunctionalVarietyTracker.Controls.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary x:Key="ItemsControlSource"/>
      <ResourceDictionary x:Key="ItemsControlResource"/>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="20" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Slider Grid.Row="0" />

    <StackPanel Grid.Row="1">

      <ItemsControl ItemsSource="{StaticResource ItemsControlSource}"
                    DataContext="{StaticResource ItemsControlResource}"/>

    </StackPanel>
  </Grid>
</UserControl>